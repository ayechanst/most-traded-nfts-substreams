syntax = "proto3";

import "google/protobuf/timestamp.proto";

package contract.v1;

message Transfer {
    string tokenID = 1;
    string transferCount = 2;
}

message Transfers {
    repeated Transfer transfers = 1;
}